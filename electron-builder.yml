appId: com.localify.prototoy
productName: Prototoy
publish:
  - provider: github
    releaseType: release
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.*}'
  - '!build/generate-icons.js'
extraResources:
  - from: build/icon/icon-256x256.png
    to: icon.png
asarUnpack:
  - resources/**
  - node_modules/react/**
  - node_modules/react-dom/**
  - node_modules/scheduler/**
mac:
  icon: build/icon/icon.png
  category: public.app-category.developer-tools
  entitlementsInherit: build/entitlements.mac.plist
  # Self-signed code-signing identity (CN of the cert in .signing/).
  # Required for Squirrel.Mac auto-updates; the app is NOT Apple-notarized.
  identity: Prototoy Self-Signed
  # No notarization, so the hardened runtime only causes library-validation
  # crashes with a self-signed (no Team ID) cert. Disable it.
  hardenedRuntime: false
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
  target:
    - dmg
    - zip
linux:
  icon: build/icon/
  category: Development
  description: UI mockup and wireframe organizer
  maintainer: Localify
  desktop:
    entry:
      StartupWMClass: Prototoy
  target:
    - AppImage
